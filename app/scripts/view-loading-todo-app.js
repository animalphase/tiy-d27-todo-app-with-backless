export default function todosLoadingView(store) {

  let $viewContent = $(`<section class="page-wrapper view-todo">
                          <div class="todos">
                            <h2>loading todos…</h2>
                            <form
                          </div>
                        </section>`);

  return $viewContent;
}
